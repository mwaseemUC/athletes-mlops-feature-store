# Athletes MLOps Feature Store Pipeline (ADSP 31021, Assignment 2)

End-to-end reproducible ML pipeline: Feast feature store with two feature
versions, LogisticRegression trained across two hyperparameter configs,
MLflow tracking on Databricks. Four experiment combinations.

## Setup
1. Python 3.11, `python -m venv .venv && source .venv/bin/activate`
2. `pip install -r requirements.txt`
3. Set Databricks env vars (see below)

## Reproduce
(to be completed)